#[derive(Ord, PartialOrd, Eq, PartialEq, Copy, Clone)]
pub enum SceneTextureKey {
    Level0,
}

/// All textures
#[derive(Ord, PartialOrd, Eq, PartialEq, Copy, Clone)]
pub enum TextureKey {
    Player,

    // Platforms

}